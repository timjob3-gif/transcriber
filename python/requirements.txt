# Зафиксированные версии — проверено, работает.
# При обновлении — тестировать вручную перед сменой версии.
faster-whisper==1.2.1
torch==2.12.0+cpu
yt-dlp==2026.03.17

# Источник torch для CPU-only сборки:
# pip install torch==2.12.0+cpu --index-url https://download.pytorch.org/whl/cpu
# Для CUDA (RTX и др.):
# pip install torch==2.12.0+cu121 --index-url https://download.pytorch.org/whl/cu121
